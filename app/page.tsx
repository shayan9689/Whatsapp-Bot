export default function Home() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui" }}>
      <h1>WhatsApp RAG Bot</h1>
      <p>API-only. Webhook: <code>/api/webhook</code></p>
    </main>
  );
}
